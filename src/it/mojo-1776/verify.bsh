import java.io.*;
import java.util.*;
import java.util.jar.*;
import java.util.regex.*;

try
{
    File targetDir = new File( basedir, "target" );
    File targetFile = new File ( targetDir, "nsis-maven-plugin.mojo1776-0.0.1-SNAPSHOT-quickinstall.exe" );
    if ( !targetFile.exists() )
    {
        System.out.println( "FAILURE! " );
        return false;
    }
}
catch( Throwable t )
{
    t.printStackTrace();
    return false;
}

return true;
